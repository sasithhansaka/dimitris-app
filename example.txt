@import "tailwindcss";

/* ==========================================================================
   PERKS — design tokens
   Ruled retail broadsheet: warm paper ground, hairline rules carry structure,
   the saving is the display type. Shadows appear once (hover lift), never as
   the resting state of a surface.
   ========================================================================== */

@theme {
  /* Ground & surfaces */
  --color-paper: #fbfaf7;
  --color-paper-deep: #f2f0e9;
  --color-surface: #ffffff;

  /* Ink */
  --color-ink: #14151c;
  --color-ink-2: #4a4d5a;
  --color-ink-3: #6b6f7e;
  --color-ink-inv: #fbfaf7;

  /* Rules */
  --color-rule: #e7e3da;
  --color-rule-strong: #d6d1c4;

  /* Primary — electric cobalt */
  --color-primary: #1e45e8;
  --color-primary-hover: #1734bd;
  --color-primary-ink: #142f9e;
  --color-primary-tint: #ebeefe;

  /* Savings — controlled fresh green */
  --color-save: #0b7c55;
  --color-save-ink: #086545;
  --color-save-tint: #e3f2eb;

  /* Urgency — warm coral, used sparingly */
  --color-urgent: #dc5533;
  --color-urgent-ink: #b03e20;
  --color-urgent-tint: #fbede6;

  /* Type */
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --font-display: var(--font-archivo), var(--font-inter), ui-sans-serif, system-ui, sans-serif;

  /* Radius — restrained */
  --radius-xs: 4px;
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 14px;
  --radius-xl: 20px;

  /* Layout */
  --spacing-header: 68px;
  --spacing-shell: 1340px;

  /* Motion */
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
}

@layer base {
  html {
    -webkit-tap-highlight-color: transparent;
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--color-paper);
    color: var(--color-ink);
    font-family: var(--font-sans);
    font-feature-settings: "cv05" 1, "cv08" 1, "ss03" 1;
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeLegibility;
    overflow-x: clip;
  }

  /* Browser surfaces we did not draw, themed from the palette */
  ::selection {
    background-color: color-mix(in oklab, var(--color-primary) 18%, transparent);
    color: var(--color-ink);
  }

  :root {
    accent-color: var(--color-primary);
    caret-color: var(--color-primary);
    color-scheme: light;
  }

  *:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
    border-radius: 3px;
  }

  /* Firefox */
  * {
    scrollbar-color: var(--color-rule-strong) transparent;
    scrollbar-width: thin;
  }

  /* WebKit */
  ::-webkit-scrollbar {
    width: 11px;
    height: 11px;
  }
  ::-webkit-scrollbar-track {
    background: transparent;
  }
  ::-webkit-scrollbar-thumb {
    background-color: var(--color-rule-strong);
    border: 3px solid var(--color-paper);
    border-radius: 999px;
  }
  ::-webkit-scrollbar-thumb:hover {
    background-color: var(--color-ink-3);
  }

  a {
    text-underline-offset: 0.22em;
    text-decoration-thickness: from-font;
  }

  input,
  textarea,
  select,
  button {
    font: inherit;
    color: inherit;
  }

  input::placeholder,
  textarea::placeholder {
    color: var(--color-ink-3);
  }

  /* Remove the UA search-field affordances; we draw our own */
  input[type="search"]::-webkit-search-cancel-button {
    -webkit-appearance: none;
    appearance: none;
  }
}

@layer components {
  /* Tracked micro-label — the broadsheet's metadata voice */
  .u-label {
    font-size: 0.6875rem;
    line-height: 1;
    font-weight: 600;
    letter-spacing: 0.09em;
    text-transform: uppercase;
    font-variant-numeric: tabular-nums;
  }

  /* Display numerals for savings values */
  .u-value {
    font-family: var(--font-display);
    font-variant-numeric: tabular-nums lining-nums;
    letter-spacing: -0.035em;
    font-weight: 800;
  }

  .u-display {
    font-family: var(--font-display);
    letter-spacing: -0.035em;
    font-weight: 700;
    text-wrap: balance;
  }

  .u-nums {
    font-variant-numeric: tabular-nums;
  }

  /* Horizontal rails: hide the bar, keep the scroll */
  .u-rail {
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .u-rail::-webkit-scrollbar {
    display: none;
  }

  /* Edge fade for rails at the shell boundary */
  .u-rail-mask {
    -webkit-mask-image: linear-gradient(to right, transparent 0, #000 18px, #000 calc(100% - 40px), transparent 100%);
    mask-image: linear-gradient(to right, transparent 0, #000 18px, #000 calc(100% - 40px), transparent 100%);
  }

  /* Same edge fade, released at the breakpoint where the rail becomes a grid. */
  .u-rail-mask-sm {
    -webkit-mask-image: linear-gradient(to right, transparent 0, #000 18px, #000 calc(100% - 40px), transparent 100%);
    mask-image: linear-gradient(to right, transparent 0, #000 18px, #000 calc(100% - 40px), transparent 100%);
  }

  @media (min-width: 640px) {
    .u-rail-mask-sm {
      -webkit-mask-image: none;
      mask-image: none;
    }
  }

  @media (min-width: 1024px) {
    .u-rail-mask {
      -webkit-mask-image: none;
      mask-image: none;
    }
  }

  /* The one shadow in the system: a 1px hover lift on interactive tiles */
  .u-lift {
    transition:
      transform 220ms var(--ease-out-expo),
      box-shadow 220ms var(--ease-out-expo),
      border-color 220ms var(--ease-out-expo);
  }
  @media (hover: hover) {
    .u-lift:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 18px -8px rgb(20 21 28 / 0.16);
      border-color: var(--color-rule-strong) !important;
    }
  }
}

@keyframes perks-pop {
  0% { transform: scale(1); }
  42% { transform: scale(1.28); }
  100% { transform: scale(1); }
}

@keyframes perks-sheet-in {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

@keyframes perks-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes perks-toast-in {
  from { opacity: 0; transform: translateY(14px) scale(0.97); }
  to { opacity: 1; transform: none; }
}

.animate-pop { animation: perks-pop 340ms var(--ease-out-expo); }
.animate-sheet-in { animation: perks-sheet-in 300ms var(--ease-out-expo); }
.animate-fade-in { animation: perks-fade-in 200ms ease-out; }
.animate-toast-in { animation: perks-toast-in 300ms var(--ease-out-expo); }

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
  .u-lift:hover { transform: none; }
}
